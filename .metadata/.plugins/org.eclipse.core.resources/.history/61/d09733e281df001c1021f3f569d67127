package br.com.checkpoint.view;

import java.util.Scanner;

import javax.swing.JOptionPane;

import br.com.checkpoint.model.Computador;
import br.com.checkpoint.model.Endereco;
import br.com.checkpoint.model.Jogos;
import br.com.checkpoint.model.Produto;
import br.com.checkpoint.model.Telefone;
import br.com.checkpoint.model.Venda;
import br.com.checkpoint.model.VideoGame;

public class Loja {

	public static void main(String[] args) {

		// Instanciando leitor
		Scanner leitor = new Scanner(System.in);

		// Instanciando endereço
		Endereco enderco = new Endereco("Avenida Lins de Vasconcelos ", 1220, " - Aclimação", " - São Paulo");

		// Instanciado telefone
		Telefone telefone = new Telefone(943302828, 40028000);

		// Instanciando produto
		Produto produto = new Produto();

		// Instanciando jogos
		Jogos game = new Jogos();

		// Instanciado computador
		Computador computador = new Computador();

		// Instanciado video game
		VideoGame video = new VideoGame("PS5",1400.00,100,"controle-lilas");

		// Instanciado venda
		Venda venda = new Venda();

//		String cliente = JOptionPane.showInputDialog("Digite o nome do cliente: ");
//		String p = JOptionPane.showInputDialog("Digite o produto que deseja comprar: ");
//		int qtd = Integer.parseInt(JOptionPane.showInputDialog("Digite a quantidade: "));

		// Instanciando o metodo de valor a ser pago pelo cliente
		System.out.println("Qual seu nome? ");
		String n = leitor.nextLine();
		venda.setCliente(n);
		
		System.out.println("Qual produto você quer comprar? ");
		String compra = leitor.nextLine();
		produto.setNome(compra);
		
		System.out.println("Qual a quantidade que deseja comprar? ");
		int qtd = leitor.nextInt();
		venda.setQuantidade(qtd);
		
		
		System.out.println(venda.getCliente());
		System.out.println(produto.getNome());
		System.out.println(venda.getQuantidade() * produto.getPreco());
		
//		double total = leitor.nextDouble();
//		
//		
//		
//		
//		
//		
//
//		System.out.println("Total da compra: " + total);
//		
//		System.out.println(venda);
	}

}
